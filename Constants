// if you don't want others or yourself to overwrite existing values you can add the const keyword infront of variable type
const int myNum = 15;
mynum = 20; // error: this cannot occur b/c of the const keyword
